import '../engine/tile.dart';
import 'component.dart';
class StoneComponent extends Component<Tile> {
  StoneComponent(Tile parent) : super(parent);

  @override
  void update() {
    // Logic if any; might be passive
  }
}